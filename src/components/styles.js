const styles = (theme) => ({
  dashboardCardTitle: {
    padding: `12px ${theme.padding.sm.px}`,
    border: '1px solid #000',
    background: '#585AFA',
    textAlign: 'center',
    width: '300',
  },
});

export default styles;
